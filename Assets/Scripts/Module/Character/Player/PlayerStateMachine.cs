using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerStateMachine : CharacterStateMachine<Player>
{
    public PlayerStateMachine(Player owner) : base(owner)
    {
        
    }
}
